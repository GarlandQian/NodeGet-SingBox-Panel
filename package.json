{
  "name": "nodeget-singbox-panel",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "NodeGet Dashboard sing-box configuration extension",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "clean": "node scripts/clean.mjs",
    "build": "node scripts/clean.mjs && vite build",
    "package": "node scripts/clean.mjs && vite build && node scripts/package.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "check": "node scripts/clean.mjs && vite build"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.6",
    "vite": "^8.0.8"
  }
}
